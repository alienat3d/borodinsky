FIXME:
-> Remove BEM.

TODO:
-> Modal-map pops-up on <address> click.

-> Проверить текст и заменить "е" на "ё".

-> Страничка-fallback с формой ввода логина и пароля "login.html" на случай, если не работает JS.

-> Реализовать на JS, чтобы по клику вне модального окна оно закрывалось.

-> Реализовать на JS, чтобы по нажатию кнопки "Esc" закрывались модальные окно.

-> При загрузки login.html фокус должен падать на input login.